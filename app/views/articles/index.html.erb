
<h1 class="text-center text-2xl font-bold text-slate-600 mt-4">Articles</h1>

<p class="text-center text-slate-500">Our blog has <%= Article.public_count %> articles and counting!</p>

<ul class="my-4 flex flex-col items-start justify-center">
    <% @articles.each do |article| %>
        <% unless article.archived? %>
    
            <li class="mb-2">
                <%= link_to article.title, article, class: "text-blue-500 hover:underline" %>
            </li>

        <% end %>

    <% end %>
</ul>

<!-- <%= link_to "New Article", new_article_path, class: "px-4 py-2 bg-yellow-400 text-white rounded-lg hover:bg-yellow-500" %> -->


